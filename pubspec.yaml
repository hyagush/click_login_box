name: click_login_box
description: Package realizado para utilizar a Login Page nos projetos da Click.
version: 1.0.3
homepage: https://github.com/hyagush/click_login_box

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: